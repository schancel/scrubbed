The following function keeps only nonempty labels.

```python
# SPDX-License-Identifier: Apache-2.0
def keep_labels(values):
    return [value for value in values if value.strip()]
```

The surrounding paragraph is part of the document, not the program.
